#include <iostream> 
#include <conio.h>
#include <math.h>
using namespace std;

int main ()
{
    float a,S,V;
    cout << "a = ";
    cin >>a;
    S = 6 * a*a;
    V = pow(a,3);
    cout << "S = "<<S<<"\n"<<"V = "<<V;

	getch();
	return 0;
 } 
